const taskFirst() {
    var task = 'I prefer const when I can.';
    return task;
}

export function getLast() {
    return ' is okay';
}

let taskNext() => {
    var combination = 'But seomtimes let';
    combination += getLast();

    return combination;
}
